server.port=8888
spring.application.name=config-server

# Native file system configuration
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=file:///Users/ADML/Desktop/microservices /config-repo

# Disable Eureka registration for Config Server (it starts before Eureka)
spring.cloud.config.discovery.enabled=false

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,refresh

# Logging
logging.level.org.springframework.cloud.config=DEBUG
logging.level.com.ncu.config=DEBUG
